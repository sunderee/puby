name: puby
description: Dart-based CLI app for managing dependencies in pubspec.yaml
version: 0.0.1
publish_to: "none"

environment:
  sdk: ">=2.19.0-444.2.beta <3.0.0"

dependencies:
  args: ^2.3.1

dev_dependencies:
  lints: ^2.0.1
  test: ^1.22.1
